{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "启动 PWA 开发服务器",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/node_modules/.bin/live-server",
            "args": ["--port=3000", "--host=localhost", "--open=/"],
            "console": "integratedTerminal",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}
